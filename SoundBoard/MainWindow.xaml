<Controls:MetroWindow x:Class="SoundBoard.MainWindow"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoundBoard"
        mc:Ignorable="d"
        Title="{local:Loc AppTitle}" Height="736.011" Width="664.541"
        SaveWindowPosition="True"
    
        BorderThickness="1" 
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        ResizeMode="CanResizeWithGrip"
        Loaded="Window_Loaded"
        OverlayFadeIn="{StaticResource OverlayFastSemiFadeIn}"
        OverlayFadeOut="{StaticResource OverlayFastSemiFadeOut}"
        UseLayoutRounding="True">
    <!-- UseLayoutRounding dramatically improves the appearance of small images. -->

    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>

            <Controls:Flyout x:Name="Search" Header="{local:Loc SearchHeader}" ClosingFinished="FlyoutCloseHandler" Position="Left" Width="300" >
                <DockPanel x:Name="SearchPanel" >
                    <TextBox DockPanel.Dock="Top" x:Name="Query" IsReadOnly="True" FontSize="20" ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                    <ScrollViewer VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ResultsPanel" />
                    </ScrollViewer>
                </DockPanel>
            </Controls:Flyout>

            <Controls:Flyout x:Name="Snackbar" Position="Bottom" IsAutoCloseEnabled="true" CloseButtonVisibility="Collapsed">
                <Grid x:Name="SnackbarGrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="15, 10, 50, 10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SnackbarMessage" 
                             Grid.Column="0"
                             IsReadOnly="True"
                             IsHitTestVisible="False"
                             FontSize="15"
                             BorderThickness="0"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Center"
                             TextAlignment="Center"/>
                    
                    <Button x:Name="UndoButton"
                            Style="{StaticResource MahApps.Styles.Button.Square}"
                            BorderThickness="0"
                            Grid.Column="2"
                            Controls:ControlsHelper.ContentCharacterCasing="Normal"
                            Content="{local:Loc Undo}"
                            FontSize="12"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="20, 0, 20, 0"
                            Click="UndoButton_Click"/>
                    
                    <Button x:Name="CloseSnackbarButton" 
                            Style="{StaticResource MahApps.Styles.Button.Square}"
                            BorderThickness="0"
                            Grid.Column="3"
                            FontSize="20"
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="CloseSnackbarButton_Click"/>
                </Grid>
            </Controls:Flyout>

        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>

    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="{local:Loc SilenceButton}" Click="silence_Click" Name="Silence" ToolTip="{local:Loc SilenceButtonToolTip}"/>
            <Button Content="{local:Loc RenamePageButton}" Click="renamePage_Click" Name="Rename"/>
            <Button Content="{local:Loc RemovePageButton}" Click="removePage_Click" Name="Remove"/>
            <Button Content="{local:Loc AddPageButton}" Click="addPage_Click"/>
            <Button Content="{local:Loc HelpButton}" Click="help_Click" Name="Help"/>
            <Button Content="{local:Loc AboutButton}" Click="about_Click"/>
            <Button Content="•••" Click="overflow_Click" Name="Overflow"/>
            <Border Name="AdminBorder" BorderThickness="1" BorderBrush="LightBlue" Margin="5" Padding="5,0,5,0"
                    ToolTip="{local:Loc RunningAsAdminToolTip}">
                <TextBlock Name="AdminText" Text="{local:Loc RunningAsAdmin}" VerticalAlignment="Center" Foreground="LightBlue" />
            </Border>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>

    <Controls:MetroWindow.CommandBindings>
        <CommandBinding Command="{x:Static local:ShortcutCommands.AboutBoxCommand}" Executed="AboutBoxCommand_Executed"/>
    </Controls:MetroWindow.CommandBindings>

    <Grid>
        <TabControl xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls" Name="Tabs"
                    Style="{StaticResource MahApps.Styles.TabControl.AnimatedSingleRow}">
            <!-- Must be a MyMetroTabItem: LoadSettings casts this placeholder when building the welcome page. -->
            <local:MyMetroTabItem>
            </local:MyMetroTabItem>
        </TabControl>

    </Grid>
</Controls:MetroWindow>
