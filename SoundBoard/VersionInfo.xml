<?xml version="1.0" encoding="utf-8" ?>
<AppUpdate xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/jborgese/SoundBoard/master/SoundBoard/AppUpdate.xsd">
  <!-- GENERATED by scripts/New-VersionInfo.ps1 from tag v1.11.0 - do not edit by hand. -->
  <Version>1.11.0.0</Version>
  <ReleaseDate>2026-08-23</ReleaseDate>
  <!-- Default download -->
  <DownloadLink>https://github.com/jborgese/SoundBoard/releases/download/v1.11.0/SoundBoard.exe</DownloadLink>
  <DownloadFileName>SoundBoard.exe</DownloadFileName>
  <!-- All download options -->
  <Downloads>
    <Download key="portable">
      <Link>https://github.com/jborgese/SoundBoard/releases/download/v1.11.0/SoundBoard.exe</Link>
      <FileName>SoundBoard.exe</FileName>
      <FileHash algorithm="SHA256">15D2854780113DAAEB94FE6B5281FDDF55A781A358CD6101FCD113CF720A64DF</FileHash>
    </Download>
  </Downloads>
  <!-- Release notes -->
  <VersionNotes> - File logging to `%AppData%\SoundBoard\soundboard.log` and an &quot;Open log folder&quot; menu item.
 - Reproducible build documentation (`BUILDING.md`), a CI build on every push, and unit tests for the model, serializer, utilities and audio device handling.
 - Tag-driven releases: the version is stamped from the `v*` git tag, `VersionInfo.xml` is generated (with a SHA-256 `&lt;FileHash&gt;` so the updater verifies downloads), and the GitHub Release is created automatically.
 - A `LICENSE` file (MIT, which the README already claimed), `CONTRIBUTING.md`, GitHub issue and pull request templates, and `docs/update-manifest.md` describing the `VersionInfo.xml` update-manifest format and how the in-app updater consumes it.
 - Support for more than one interface language, and a **Language** submenu under **•••** to choose one. The choice is saved as `Language` in `soundboard.config` alongside the other global settings; the default, and the &quot;Same as Windows&quot; entry, follow the operating system. Changing it applies on the next start, which the app offers to do for you.
 - A Spanish translation. Translations are compiled into satellite assemblies that are embedded in the single-file `SoundBoard.exe` along with everything else, so nothing extra ships next to it. See [Localization](BUILDING.md#localization) for how to add a language, and `SoundBoard.Tests/LocalizationTests.cs` for the checks that keep translations honest (no missing strings, no stale ones, and the same `{0}` placeholders as the English).
 - A **Theme** submenu under **•••** offering a Light and a Dark theme. Unlike the language it applies immediately with no restart, and the choice is saved as `Theme` in `soundboard.config` so it is still there the next time you start.
 - A row of controls along the bottom of every sound: **play/pause**, **stop**, **loop**, **mute**, **solo** and **remove**, each with a tool tip. They are always there rather than appearing only while a sound plays, so a sound can be started, looped or cleared without going through the **···** menu. Remove is undoable through the usual snackbar, and all six apply to the whole selection when several sounds are selected, exactly as the menu does.
 - **Mute** silences one sound without stopping it — it keeps its place, keeps its progress bar and still chains to its next sound — and takes effect immediately on a sound that is already playing. It is saved with the sound, so a muted sound comes back muted.
 - **Solo** silences everything that is not soloed, for auditioning one sound against a busy board. Any number of sounds can be soloed at once. Unlike mute it is not saved, so the board never starts up silent for a reason you cannot see.
 - Target framework unified at .NET Framework 4.8 across all projects.
 - The last of the user-facing text that was still hard-coded — the title bar buttons, the **ADMIN** badge, the search flyout, the snackbar&apos;s **UNDO**, the &quot;Set Hotkeys&quot; and &quot;Change Button Grid&quot; dialogs, the &quot;All files&quot; entry in the sound browser, and the reasons the updater gives for rejecting a download — now comes from the resource file like everything else.
 - The loop indicator in a sound&apos;s bottom-right corner is gone: looping is now shown and toggled by the loop control on the new row of controls. The volume-offset indicator moves down into the space it leaves.
 - The &quot;hotkey already in use&quot; warnings are now whole sentences joined at display time, instead of fragments concatenated in a fixed order with trailing spaces baked into the resource strings.
 - Build: NuGet dependencies moved from `packages.config` to `PackageReference` (`msbuild /restore`; no `nuget.exe` or `packages\` folder needed), Fody 5.1.1 → 6.9.3 and Costura.Fody 4.0.0 → 6.2.0.
 - Dependencies: System.Reactive 5.0.0 → 6.1.0, MouseKeyHook 5.6.0 → 5.7.1, NAudio 1.9.0 → 2.2.1, MahApps.Metro 1.6.5 → 2.4.11 (with ControlzEx 5.0.2 and MahApps.Metro.SimpleChildWindow 2.2.1). The UI looks the same apart from slightly wider Windows-10-style title bar buttons.
 - The two prebuilt third-party DLLs (`Dsafa.WpfColorPicker.dll`, `HotKeyManagement.WPF.4.dll`) are now built from vendored source projects (MIT; licences and a NOTICE of local changes are included alongside the source).
 - README screenshots are served from `docs/images/` in this repository instead of third-party image hosts, and the README documents how to build and run from source.
 - Extended.Wpf.Toolkit (Xceed) dependency: the row/column spinners in the &quot;Change button grid&quot; dialog are now MahApps `NumericUpDown` controls (newer Xceed versions are under a non-commercial license, and 3.x is unmaintained).
 - Internal refactor: the configuration model lives in `SoundBoard.Model` and is the live source of truth for the UI; undo works from in-memory snapshots; the playback engine was extracted from `SoundButton`; hotkeys and search operate on the model; the `MainWindow.Instance` singleton was retired.
 - The in-app updater now refuses any download that does not match a SHA-256 `&lt;FileHash&gt;` in the update manifest (a missing or empty hash is a failure, not a pass).
 - The update is applied in-process by renaming the running exe, instead of through an elevated `cmd.exe` / `powershell.exe` command line built from file paths. UAC is only requested when the exe&apos;s folder is not writable, and the elevated mode (`SoundBoard.exe --apply-update &lt;file&gt; &lt;sha256&gt;`) can only replace its own image with a file matching the given hash.
 - Updating no longer kills the app with `taskkill /f`, so unsaved settings are written before the new version starts.
 - Crash on first launch when no configuration file exists.
 - Progress-bar update loop that never terminated after a sound stopped.
 - Tabs, tab items and menu items leaking through static dictionaries.
 - The config file that actually failed to load is now the one that gets backed up.
 - An unparseable audio device ID is no longer treated as the default device.
 - `Truncate` running off the end of short strings.</VersionNotes>
  <ReleaseNotes></ReleaseNotes>
</AppUpdate>